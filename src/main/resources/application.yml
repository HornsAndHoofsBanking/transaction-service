spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/banking
    username: me
    password: me
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 9090